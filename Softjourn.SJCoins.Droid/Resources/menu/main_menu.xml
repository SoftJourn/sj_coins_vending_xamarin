<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

  <item
      android:id="@+id/action_search"
      android:icon="@android:drawable/ic_menu_search"
      android:title="@string/menu_item_search"
      android:visible="false"
      app:actionViewClass="android.support.v7.widget.SearchView"
      app:showAsAction="ifRoom" />

  <item
      android:id="@+id/menu_favorites"
      android:title="@string/favorites"
      android:icon="@drawable/ic_favorite_white_24dp"
      app:showAsAction="ifRoom"/>

  <item
      android:id="@+id/profile"
      android:title="@string/menu_bottombar_profile"
      android:icon="@drawable/ic_account_circle_white_24dp"
      app:showAsAction="ifRoom"/>

  <item
      android:id="@+id/menu_buy"
      android:title="@string/menu_buy"
      android:icon="@drawable/ic_basket_white_6"
      app:showAsAction="ifRoom"/>

  <item
      android:id="@+id/menu_add_favorite"
      android:title="@string/menu_add_favorites"
      android:icon="@drawable/ic_favorite_border_white"
      app:showAsAction="ifRoom"/>
</menu>